action_endpoint:
  url: "http://localhost:5055/webhook"

tracker_store:
  type: sql
  dialect: postgresql
  host: ${RASA_DB_HOST}
  port: ${RASA_DB_PORT}
  username: ${RASA_DB_USER}
  password: ${RASA_DB_PASSWORD}
  db: ${RASA_DB_NAME}